module.exports = function(grunt) {
  grunt.initConfig({
    // configurações aqui
  });

    // Área da tarefa
    grunt.registerTask('hello', function() {
    console.log('Olá, Grunt!');
    });
    // fim da área da tarefa
}

